import GroupBoard from '@components/groups/GroupBoard';
import Layout from '@components/layout/Layout';

const Main = () => {
  return (
    <Layout>
      <GroupBoard />
    </Layout>
  );
};

export default Main;
